#include <iostream>
#include <string>

using namespace std;

int main()
{
    string t = "This is a text to see how often the word \"the\" occurs.  The simple function does not deal with capital letters, the word we're looking for has to be exacly the same as the word in the text.";

    cout << t << endl;
    return 0;
}
